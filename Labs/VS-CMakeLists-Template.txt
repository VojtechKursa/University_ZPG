cmake_minimum_required (VERSION 3.8)

# Enable Hot Reload for MSVC compilers if supported.
if (POLICY CMP0141)
  cmake_policy(SET CMP0141 NEW)
  set(CMAKE_MSVC_DEBUG_INFORMATION_FORMAT "$<IF:$<AND:$<C_COMPILER_ID:MSVC>,$<CXX_COMPILER_ID:MSVC>>,$<$<CONFIG:Debug,RelWithDebInfo>:EditAndContinue>,$<$<CONFIG:Debug,RelWithDebInfo>:ProgramDatabase>>")
endif()



project ("Lab_02")



# Add source to this project's executable.
add_executable (main
	"Lab_02.cpp" "Lab_02.h")



if (CMAKE_VERSION VERSION_GREATER 3.12)
  set_property(TARGET Lab_02 PROPERTY CXX_STANDARD 20)
endif()



# Libraries

# GLM
target_include_directories(main ../../Libraries/glm/)


# GLFW
add_subdirectory(../../Libraries/glfw ../../Libraries/glfw)
add_dependencies(main glfw)

target_include_directories(main ../../Libraries/glfw/include)

target_link_libraries(main glfw)


# OpenGL
find_package(OpenGL REQUIRED)

target_include_directories(main ${OPENGL_INCLUDE_DIRS})

target_link_libraries(main ${OPENGL_LIBRARIES})


# GLU
# find_package(GLU REQUIRED)

# target_include_directories(main ${GLU_INCLUDE_DIRS})

# target_link_libraries(main ${GLU_LIBRARY})


# GLEW
add_subdirectory(../../Libraries/glew)
add_dependencies(main glew)

target_include_directories(main ../../Libraries/glew/include)

target_link_libraries(main glew)
